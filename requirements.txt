# Núcleo de la app — verificado con streamlit 1.57 / pandas 3.0 / Python 3.14.
# Cotas mínimas (>=) para que sirvan tanto entornos antiguos como los actuales.
streamlit>=1.32
pandas>=2.1
numpy>=1.26
plotly>=5.18
openpyxl>=3.1
python-dateutil>=2.8
pytz>=2023.3

# Extras opcionales (no los usa el núcleo actual; se conservan por compatibilidad)
scipy>=1.11
scikit-learn>=1.3
Pillow>=10.1
requests>=2.31
python-dotenv>=1.0
